import { ssrRenderAttrs as c } from 'vue/server-renderer'
import { useSSRContext as n } from 'vue'
import { _ as l } from './plugin-vue_export-helper.CTtO9zSR.js'
;(function () {
  try {
    var e =
      typeof window < 'u'
        ? window
        : typeof global < 'u'
          ? global
          : typeof globalThis < 'u'
            ? globalThis
            : typeof self < 'u'
              ? self
              : {}
    e.SENTRY_RELEASE = { id: '63d551497be1158ac55eeefaf1dec4a9182e3c76' }
    var o = new e.Error().stack
    o &&
      ((e._sentryDebugIds = e._sentryDebugIds || {}),
      (e._sentryDebugIds[o] = '32b226f2-f7e9-4f09-9d5e-5d90529f7131'),
      (e._sentryDebugIdIdentifier =
        'sentry-dbid-32b226f2-f7e9-4f09-9d5e-5d90529f7131'))
  } catch {}
})()
const p = JSON.parse(
    '{"title":"React.js 框架","description":"","frontmatter":{"outline":[2,3]},"headers":[],"relativePath":"frameworks/react/basic/intro.md","filePath":"frameworks/react/basic/intro.md","lastUpdated":1782271806000}',
  ),
  a = { name: 'frameworks/react/basic/intro.md' }
function i(e, o, t, r, d, u, f, h) {
  o(
    `<div${c(r)}><h1 id="react-js-框架" tabindex="-1">React.js 框架 <a class="header-anchor" href="#react-js-框架" aria-label="Permalink to &quot;React.js 框架&quot;">​</a></h1><p>在前端开发的世界里，React 凭借其“声明式、组件化、Learn Once Write Anywhere”的设计哲学，定义了现代前端开发的范式。它的核心思想可以用一个极其优雅的公式概括：<code>UI = f(state)</code>（视图是状态的映射）。无论 React 如何从 Class 时代演进到 Hooks 时代，甚至步入并发渲染模式，其核心始终围绕着<strong>单向数据流</strong>与<strong>不可变性 (Immutability)</strong>。</p><h2 id="_1-核心基石-jsx-与-单向数据流" tabindex="-1">1. 核心基石：JSX 与 单向数据流 <a class="header-anchor" href="#_1-核心基石-jsx-与-单向数据流" aria-label="Permalink to &quot;1. 核心基石：JSX 与 单向数据流&quot;">​</a></h2><h3 id="_1-1-jsx-javascript-的语法糖" tabindex="-1">1.1 JSX：JavaScript 的语法糖 <a class="header-anchor" href="#_1-1-jsx-javascript-的语法糖" aria-label="Permalink to &quot;1.1 JSX：JavaScript 的语法糖&quot;">​</a></h3><ul><li><strong>本质</strong>：JSX 并不是 HTML，它最终会被 Babel 或 SWC 编译成 <code>React.createElement()</code>（或新的 <code>_jsx</code> 函数）调用，生成纯粹的 JavaScript 对象（即 Virtual DOM）。</li><li><strong>优势</strong>：赋予了 UI 描述以完整的图灵完备能力。你可以直接在视图层使用 <code>map</code> 渲染列表、使用三元运算符进行条件渲染，彻底打破了传统模板引擎的局限。</li></ul><h3 id="_1-2-单向数据流-one-way-data-flow" tabindex="-1">1.2 单向数据流 (One-way Data Flow) <a class="header-anchor" href="#_1-2-单向数据流-one-way-data-flow" aria-label="Permalink to &quot;1.2 单向数据流 (One-way Data Flow)&quot;">​</a></h3><ul><li>React 强制规定数据只能从父组件流向子组件。</li><li><strong>🚨 核心红线</strong>：<strong>绝对不要直接修改 State（和 Props）</strong>！无论是 Class 组件里的 <code>this.state</code> 还是 Hooks 里的 <code>state</code>。React 强调<strong>不可变性</strong>，必须通过 <code>setState</code> 或 <code>dispatch</code> 来生成一个全新的引用，React 才能感知到变化并触发重新渲染。</li></ul><h2 id="_2-核心范式-hooks-与-副作用管理" tabindex="-1">2. 核心范式：Hooks 与 副作用管理 <a class="header-anchor" href="#_2-核心范式-hooks-与-副作用管理" aria-label="Permalink to &quot;2. 核心范式：Hooks 与 副作用管理&quot;">​</a></h2><p>自从 React 16.8 引入 Hooks 以后，函数组件彻底翻身，成为了处理状态和副作用的核心范式。</p><h3 id="_2-1-state-与衍生数据-usestate-usememo" tabindex="-1">2.1 State 与衍生数据 (useState &amp; useMemo) <a class="header-anchor" href="#_2-1-state-与衍生数据-usestate-usememo" aria-label="Permalink to &quot;2.1 State 与衍生数据 (useState &amp; useMemo)&quot;">​</a></h3><ul><li><strong><code>useState</code> / <code>useReducer</code></strong>：用于声明组件的内部状态。当你调用 set 函数时，React 会把组件加入更新队列，并在下一次渲染时返回最新的状态值。</li><li><strong><code>useMemo</code> (类似 Vue 的 computed)</strong>：<strong>自带缓存</strong>。由于函数组件每次渲染都会从头执行，<code>useMemo</code> 可以缓存昂贵的计算结果。如果依赖项数组 (<code>deps</code>) 没有变化，它就会直接返回上一次的缓存值。</li><li><strong>滥用警告</strong>：不要给所有变量都套上 <code>useMemo</code>，缓存本身也是有内存和对比开销的！只在进行大规模计算或为了保持引用稳定以配合 <code>React.memo</code> 时使用。</li></ul><h3 id="_2-2-effect-与-生命周期-useeffect-useeffectevent" tabindex="-1">2.2 Effect 与 生命周期 (useEffect &amp; useEffectEvent) <a class="header-anchor" href="#_2-2-effect-与-生命周期-useeffect-useeffectevent" aria-label="Permalink to &quot;2.2 Effect 与 生命周期 (useEffect &amp; useEffectEvent)&quot;">​</a></h3><ul><li><strong><code>useEffect</code> -&gt; 与外部系统同步</strong>：它是逃生舱。用于处理如网络请求、DOM 订阅、定时器等“副作用”。</li><li><strong>依赖数组 (Deps)</strong>：这是 Hooks 最容易翻车的地方。如果依赖漏写，会拿到旧数据（闭包陷阱）；如果乱写，会导致无限循环。</li><li><strong>高级进阶 (<code>useEffectEvent</code>)</strong>：React 官方正在推进的实验性 Hook。完美解决了“我想在 Effect 里读取最新的状态，但又不想因为这个状态的改变而重新触发 Effect 重新执行”的痛点，将响应式依赖与非响应式逻辑彻底解耦。</li></ul><h2 id="_3-核心进阶-virtual-dom-与-fiber-架构" tabindex="-1">3. 核心进阶：Virtual DOM 与 Fiber 架构 <a class="header-anchor" href="#_3-核心进阶-virtual-dom-与-fiber-架构" aria-label="Permalink to &quot;3. 核心进阶：Virtual DOM 与 Fiber 架构&quot;">​</a></h2><p><strong>为什么 React 渲染庞大应用依然流畅？</strong> 答案在于其底层的架构演变。</p><ol><li><strong>Virtual DOM (虚拟 DOM)</strong>：用轻量级的 JS 对象描述真实 DOM，屏蔽了跨平台的差异。</li><li><strong>Diff 算法 (协调 Reconciliation)</strong>：采用同层比较的启发式算法。遇到不同类型的节点直接销毁重建；遇到相同节点则对比属性更新。</li><li><strong>React Fiber 架构 (16 之后)</strong>：<strong>可中断的异步渲染</strong>。传统的递归 Diff 会阻塞主线程，导致浏览器掉帧卡顿。Fiber 将渲染任务拆分成一个个微小的工作单元，利用浏览器的空闲时间去计算（类似链表的遍历），计算完毕后再统一 Commit 到 DOM 上。</li></ol><p><strong>🔑 并发模式 (Concurrent Features)</strong>： 有了 Fiber 打底，React 18 引入了并发特性。例如 <strong><code>useTransition</code></strong>，它可以将某些状态更新标记为“非紧急（低优先级）”。在处理复杂视图切换或大量数据过滤时，即使后台在疯狂计算，也不会阻塞用户的点击或输入，UI 依然丝滑如顺。</p><h2 id="_4-核心架构-组件通信与逻辑复用" tabindex="-1">4. 核心架构：组件通信与逻辑复用 <a class="header-anchor" href="#_4-核心架构-组件通信与逻辑复用" aria-label="Permalink to &quot;4. 核心架构：组件通信与逻辑复用&quot;">​</a></h2><h3 id="_4-1-组件通信全家桶" tabindex="-1">4.1 组件通信全家桶 <a class="header-anchor" href="#_4-1-组件通信全家桶" aria-label="Permalink to &quot;4.1 组件通信全家桶&quot;">​</a></h3><ul><li><strong>父 -&gt; 子</strong>：<code>Props</code>。</li><li><strong>子 -&gt; 父</strong>：通过父组件传递下来的回调函数 (Callback Props) 传递数据。</li><li><strong>跨层级透传</strong>：<code>Context API</code>。解决多层级“属性钻取” (Prop Drilling) 的痛苦。</li><li><strong>全局状态</strong>：Zustand, Redux, Jotai 等。</li></ul><h3 id="_4-2-逻辑复用的终极形态-custom-hooks" tabindex="-1">4.2 逻辑复用的终极形态：Custom Hooks <a class="header-anchor" href="#_4-2-逻辑复用的终极形态-custom-hooks" aria-label="Permalink to &quot;4.2 逻辑复用的终极形态：Custom Hooks&quot;">​</a></h3><ul><li><strong>过去时代的眼泪</strong>：早期 React 依靠 Mixins、高阶组件 (HOC) 和 Render Props 来复用逻辑，导致了可怕的“嵌套地狱” (Wrapper Hell) 和 Props 命名冲突。</li><li><strong>现在的标准解法</strong>：<strong>自定义 Hook</strong>（以 <code>use</code> 开头的函数）。只需将相关的 State 和 Effect 抽离到一个纯函数中（如 <code>useFetch</code> 或 <code>useMousePosition</code>），便可实现纯粹的逻辑复用，且与 UI 渲染完美剥离，状态完全独立。</li></ul><h2 id="_5-核心周边-前端路由与状态管理" tabindex="-1">5. 核心周边：前端路由与状态管理 <a class="header-anchor" href="#_5-核心周边-前端路由与状态管理" aria-label="Permalink to &quot;5. 核心周边：前端路由与状态管理&quot;">​</a></h2><h3 id="_5-1-react-router-前端路由" tabindex="-1">5.1 React Router (前端路由) <a class="header-anchor" href="#_5-1-react-router-前端路由" aria-label="Permalink to &quot;5.1 React Router (前端路由)&quot;">​</a></h3><ul><li>提供声明式的路由配置，支持嵌套路由和动态路由。</li><li>在 React Router v6 之后，全面拥抱 Hooks API (<code>useNavigate</code>, <code>useParams</code>)，并引入了基于 Data Router 的 Loader 和 Action 机制，在路由层就解决掉数据预获取的问题。</li></ul><h3 id="_5-2-状态管理生态-百花齐放" tabindex="-1">5.2 状态管理生态 (百花齐放) <a class="header-anchor" href="#_5-2-状态管理生态-百花齐放" aria-label="Permalink to &quot;5.2 状态管理生态 (百花齐放)&quot;">​</a></h3><p>与 Vue 官方强绑定不同，React 生态极其自由：</p><ul><li><strong>Redux / RTK (Redux Toolkit)</strong>：老牌霸主，强调单一数据源和强约束的 Flux 架构。配置繁琐但可预测性极强，适合超大型应用。</li><li><strong>Zustand</strong>：极简、现代。没有样板代码，不需要用 Context 包裹整个应用，轻量级首选。</li><li><strong>Jotai / Recoil</strong>：原子化 (Atomic) 状态管理。非常适合状态极其分散、需要精准控制细粒度重渲染的场景。</li></ul><h2 id="_6-常见问题-faq-与避坑指南" tabindex="-1">6. 常见问题 (FAQ) 与避坑指南 <a class="header-anchor" href="#_6-常见问题-faq-与避坑指南" aria-label="Permalink to &quot;6. 常见问题 (FAQ) 与避坑指南&quot;">​</a></h2><h3 id="_6-1-为什么我调用了-setcount-count-1-下一行-console-log-count-打印的还是旧的值" tabindex="-1">6.1 为什么我调用了 <code>setCount(count + 1)</code>，下一行 <code>console.log(count)</code> 打印的还是旧的值？ <a class="header-anchor" href="#_6-1-为什么我调用了-setcount-count-1-下一行-console-log-count-打印的还是旧的值" aria-label="Permalink to &quot;6.1 为什么我调用了 \`setCount(count + 1)\`，下一行 \`console.log(count)\` 打印的还是旧的值？&quot;">​</a></h3><ul><li><strong>答</strong>：<strong>因为 React 的状态更新是“快照”且批处理的。</strong><ul><li>在同一个事件循环（Event Handler）中，React 会把多次 <code>setState</code> 收集起来合并，直到事件处理函数执行完毕，才会触发一次重渲染（Batching）。</li><li>在当前的渲染闭包中，<code>count</code> 的值被锁定在了渲染那一刻的快照。</li><li><strong>解法</strong>：如果下一次状态依赖于上一次状态，请使用函数式更新：<code>setCount(prev =&gt; prev + 1)</code>；如果需要在状态更新后执行逻辑，请在 <code>useEffect</code> 里监听这个状态的变化。</li></ul></li></ul><h3 id="_6-2-令人抓狂的-闭包陷阱-stale-closures-是怎么回事" tabindex="-1">6.2 令人抓狂的“闭包陷阱 (Stale Closures)”是怎么回事？ <a class="header-anchor" href="#_6-2-令人抓狂的-闭包陷阱-stale-closures-是怎么回事" aria-label="Permalink to &quot;6.2 令人抓狂的“闭包陷阱 (Stale Closures)”是怎么回事？&quot;">​</a></h3><ul><li><strong>答</strong>：这是使用 Hooks 时最常见的 Bug。 <ul><li><strong>场景</strong>：在 <code>useEffect</code> 开了一个 <code>setInterval</code> 打印 <code>count</code>，你会发现它永远打印的是初始值（比如 0）。</li><li><strong>原因</strong>：因为 <code>useEffect</code> 的依赖数组写错了（通常是写了 <code>[]</code>），导致 Effect 里的定时器回调函数永远“困”在了第一次渲染的闭包里，那个闭包里的 <code>count</code> 永远是 0。</li><li><strong>解法</strong>：1. 正确填写依赖数组 <code>[count]</code>，并在 return 中清除旧的定时器；2. 使用 <code>useRef</code> 保存最新的值，因为 ref 的 <code>current</code> 属性是可变的，不会受到闭包影响；3. 拥抱 <code>useEffectEvent</code>。</li></ul></li></ul><h3 id="_6-3-为什么列表渲染的-key-千万别用-index-索引" tabindex="-1">6.3 为什么列表渲染的 <code>key</code> 千万别用 <code>index</code>（索引）？ <a class="header-anchor" href="#_6-3-为什么列表渲染的-key-千万别用-index-索引" aria-label="Permalink to &quot;6.3 为什么列表渲染的 \`key\` 千万别用 \`index\`（索引）？&quot;">​</a></h3><ul><li><strong>答</strong>：<strong>会导致极度诡异的组件状态混乱和性能灾难。</strong><ul><li><code>key</code> 是 React 在 Diff 算法中识别节点的唯一标识。如果使用 <code>index</code>，当你在列表顶部插入一条新数据时，所有元素的 <code>index</code> 都会发生改变。</li><li>React 会认为：0号元素被修改了，1号元素被修改了...从而导致原本应该复用的真实 DOM 被全量重新修改。</li><li>更可怕的是，如果列表项是包含了不受控输入框的子组件，旧组件的内部状态（比如输入框里的字）会错误地挂载到新数据对应的组件上，发生“张冠李戴”。<strong>永远使用数据本身的唯一 ID 作为 key！</strong></li></ul></li></ul><h3 id="_6-4-useeffect-引起无限循环-死循环-的原因" tabindex="-1">6.4 <code>useEffect</code> 引起无限循环（死循环）的原因？ <a class="header-anchor" href="#_6-4-useeffect-引起无限循环-死循环-的原因" aria-label="Permalink to &quot;6.4 \`useEffect\` 引起无限循环（死循环）的原因？&quot;">​</a></h3><ul><li><strong>答</strong>：核心在于<strong>引用类型的依赖更新</strong>。 <ul><li>如果 <code>useEffect</code> 的依赖项是一个对象或数组，而在 Effect 内部又去触发状态更新导致组件重新渲染。因为每次渲染都会重新创建新的对象或数组（内存地址改变），React 比较依赖项时（<code>Object.is</code> 判断）发现它变了，于是再次执行 Effect，从而陷入死循环。</li><li><strong>解法</strong>：1. 尽量依赖基础类型（如对象的 <code>id</code>）；2. 把对象/函数的创建用 <code>useMemo</code> 或 <code>useCallback</code> 包裹起来；3. 将不需要响应式的变量抽离到组件外部。</li></ul></li></ul></div>`,
  )
}
const s = a.setup
a.setup = (e, o) => {
  const t = n()
  return (
    (t.modules || (t.modules = new Set())).add(
      'frameworks/react/basic/intro.md',
    ),
    s ? s(e, o) : void 0
  )
}
const _ = l(a, [['ssrRender', i]])
export { p as __pageData, _ as default }
//# sourceMappingURL=frameworks_react_basic_intro.md.js.map
