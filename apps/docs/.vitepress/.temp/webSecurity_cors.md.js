import { ssrRenderAttrs as n, ssrRenderAttr as s } from 'vue/server-renderer'
import { useSSRContext as i } from 'vue'
import { _ as c } from './plugin-vue_export-helper.CTtO9zSR.js'
;(function () {
  try {
    var e =
      typeof window < 'u'
        ? window
        : typeof global < 'u'
          ? global
          : typeof globalThis < 'u'
            ? globalThis
            : typeof self < 'u'
              ? self
              : {}
    e.SENTRY_RELEASE = { id: '63d551497be1158ac55eeefaf1dec4a9182e3c76' }
    var o = new e.Error().stack
    o &&
      ((e._sentryDebugIds = e._sentryDebugIds || {}),
      (e._sentryDebugIds[o] = 'ccf6c4c1-e039-450c-ace5-987e5a465589'),
      (e._sentryDebugIdIdentifier =
        'sentry-dbid-ccf6c4c1-e039-450c-ace5-987e5a465589'))
  } catch {}
})()
const l = '/CoreFront-EndConcepts/img/corsSimpleRequest.png',
  g = '/CoreFront-EndConcepts/img/corsPreflightRequest.png',
  T = JSON.parse(
    '{"title":"CORS (Cross-Origin Resource Sharing) 跨域资源共享","description":"","frontmatter":{},"headers":[],"relativePath":"webSecurity/cors.md","filePath":"webSecurity/cors.md","lastUpdated":null}',
  ),
  r = { name: 'webSecurity/cors.md' }
function h(e, o, t, d, u, p, b, P) {
  o(
    `<div${n(d)}><h1 id="cors-cross-origin-resource-sharing-跨域资源共享" tabindex="-1">CORS (Cross-Origin Resource Sharing) 跨域资源共享 <a class="header-anchor" href="#cors-cross-origin-resource-sharing-跨域资源共享" aria-label="Permalink to &quot;CORS (Cross-Origin Resource Sharing) 跨域资源共享&quot;">​</a></h1><p>在 Web 开发和前后端分离架构中，<strong>CORS（跨域资源共享）</strong> 是前端工程师和后端架构师最常遇到的网络安全与通信协商机制，也是高级面试中的必考题。虽然它经常以控制台那串刺眼的红色报错形式出现，但它的<strong>设计初衷、分类判定和防御配置策略</strong>有着极其严谨的安全逻辑。</p><h2 id="_1-简单请求-simple-requests" tabindex="-1">1. 简单请求 (Simple Requests) <a class="header-anchor" href="#_1-简单请求-simple-requests" aria-label="Permalink to &quot;1. 简单请求 (Simple Requests)&quot;">​</a></h2><p><strong>核心本质</strong>：请求完全符合传统的 Web 表单提交特征，浏览器认为此类操作是“<strong>低风险</strong>”的，因此直接发送真实的 HTTP 请求，并在收到响应后校验跨域放行权限。</p><p><strong>攻击目标</strong>：绕过浏览器的同源策略限制以获取敏感数据。</p><p><strong>一句话理解</strong>：<strong>“先斩后奏：请求直接发给服务器，如果没有跨域权限，浏览器当场扣留响应结果。”</strong></p><h3 id="_1-1-简单请求的触发条件-必须同时满足" tabindex="-1">1.1 简单请求的触发条件 (必须同时满足) <a class="header-anchor" href="#_1-1-简单请求的触发条件-必须同时满足" aria-label="Permalink to &quot;1.1 简单请求的触发条件 (必须同时满足)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>限制维度</th><th>严格要求</th></tr></thead><tbody><tr><td><strong>HTTP 方法</strong></td><td>只能是 <code>GET</code>、<code>POST</code>、<code>HEAD</code> 三者之一。</td></tr><tr><td><strong>HTTP 请求头</strong></td><td>只能包含安全的默认系统头（如 <code>Accept</code>、<code>Accept-Language</code>、<code>Content-Language</code>、<code>Last-Modified</code> 等）。<strong>绝对不允许</strong>包含任何自定义请求头（如 <code>Authorization</code>、<code>X-Token</code>）。</td></tr><tr><td><strong>Content-Type</strong></td><td>只能是以下三种传统表单格式之一：<br>1. <code>text/plain</code><br>2. <code>multipart/form-data</code> (常用于文件上传)<br>3. <code>application/x-www-form-urlencoded</code> (传统的键值对表单)</td></tr></tbody></table><h3 id="_1-2-简单请求的工作流程" tabindex="-1">1.2 简单请求的工作流程 <a class="header-anchor" href="#_1-2-简单请求的工作流程" aria-label="Permalink to &quot;1.2 简单请求的工作流程&quot;">​</a></h3><ul><li>前端发起请求，浏览器在网络层检测到该请求符合简单请求的物理特征。</li><li><strong>浏览器直接放行</strong>：浏览器不进行事前询问，直接将真实请求发送给服务器，并在请求头中自动带上来源标识 <code>Origin: http://test.com</code>。</li><li><strong>服务器真实执行</strong>：服务器接收到请求，<strong>正常执行底层的业务逻辑</strong>（如数据库的增删改查），并根据自身的跨域配置在响应头中返回 <code>Access-Control-Allow-Origin</code>。</li><li><strong>浏览器的“事后”拦截</strong>：浏览器接收到服务器的响应报文，开始核对响应头。如果响应头中没有 <code>Access-Control-Allow-Origin</code>，或者该字段的值与当前站点的域名不匹配，浏览器就会在控制台抛出 CORS 错误，并<strong>拦截响应体，绝对不把数据交给前端 JavaScript 代码</strong>。</li></ul><h3 id="_1-3-完整流程图" tabindex="-1">1.3 完整流程图 <a class="header-anchor" href="#_1-3-完整流程图" aria-label="Permalink to &quot;1.3 完整流程图&quot;">​</a></h3><p><img${s('src', l)} alt="Logo" data-zoomable="true"></p><h2 id="_2-预检请求-preflight-requests" tabindex="-1">2. 预检请求 (Preflight Requests) <a class="header-anchor" href="#_2-预检请求-preflight-requests" aria-label="Permalink to &quot;2. 预检请求 (Preflight Requests)&quot;">​</a></h2><p><strong>核心本质</strong>：当请求可能对服务器数据产生不可逆的影响，或者使用了现代 Web 的复杂数据格式（如 JSON）时，浏览器为了安全起见，必须先发送一个探测性质的请求去“<strong>探路</strong>”，征得服务器的显式同意后，才发送真正的业务请求。</p><p><strong>攻击目标</strong>：保护服务端免受未经授权的复杂跨域指令（如 <code>DELETE</code>、<code>PUT</code>）的破坏。</p><p><strong>一句话理解</strong>：<strong>“投石问路：先发个 OPTIONS 请求问问服务器行不行，服务器说行，浏览器才发真实的业务请求。”</strong></p><h3 id="_2-1-预检请求的触发条件-满足其一即可" tabindex="-1">2.1 预检请求的触发条件 (满足其一即可) <a class="header-anchor" href="#_2-1-预检请求的触发条件-满足其一即可" aria-label="Permalink to &quot;2.1 预检请求的触发条件 (满足其一即可)&quot;">​</a></h3><ul><li>使用了 <code>PUT</code>、<code>DELETE</code>、<code>PATCH</code> 等非简单 HTTP 方法。</li><li>请求体的 <code>Content-Type</code> 使用了 <code>application/json</code> 格式（现代前端主流的 API 交互格式）。</li><li>携带了自定义的 HTTP 请求头（如跨域状态下常见的 <code>Authorization: Bearer &lt;token&gt;</code>、<code>X-Requested-With</code>）。</li></ul><h3 id="_2-2-预检请求的工作流程" tabindex="-1">2.2 预检请求的工作流程 <a class="header-anchor" href="#_2-2-预检请求的工作流程" aria-label="Permalink to &quot;2.2 预检请求的工作流程&quot;">​</a></h3><ul><li>前端发起复杂的 AJAX/Fetch 请求（例如一个带有 JSON 载荷的 <code>POST</code> 请求）。</li><li>浏览器拦截该请求，<strong>暂停发送真实数据</strong>，转而向同一个目标 URL 发送一个 <strong>HTTP 方法为 <code>OPTIONS</code></strong> 的预检请求。 <ul><li>携带 <code>Access-Control-Request-Method</code>（声明接下来的真实请求会使用什么方法）。</li><li>携带 <code>Access-Control-Request-Headers</code>（声明接下来的真实请求会携带哪些自定义头）。</li></ul></li><li>服务器接收到 <code>OPTIONS</code> 请求，网关或框架中间件进行权限校验。如果同意跨域，则返回一个状态码为 204（No Content）或 200 的响应，并在响应头中附带： <ul><li><code>Access-Control-Allow-Origin</code> (允许的源)</li><li><code>Access-Control-Allow-Methods</code> (允许的方法白名单)</li><li><code>Access-Control-Allow-Headers</code> (允许的自定义头白名单)</li></ul></li><li>浏览器收到并验证 <code>OPTIONS</code> 响应通过后，<strong>才真正发起</strong>那条扣留下的业务请求。如果预检失败，真实的业务请求根本不会离开浏览器。</li></ul><h3 id="_2-3-完整流程图" tabindex="-1">2.3 完整流程图 <a class="header-anchor" href="#_2-3-完整流程图" aria-label="Permalink to &quot;2.3 完整流程图&quot;">​</a></h3><p><img${s('src', g)} alt="Logo" data-zoomable="true"></p><h2 id="_3-身份凭证与预检缓存-防线与优化" tabindex="-1">3. 身份凭证与预检缓存 (防线与优化) <a class="header-anchor" href="#_3-身份凭证与预检缓存-防线与优化" aria-label="Permalink to &quot;3. 身份凭证与预检缓存 (防线与优化)&quot;">​</a></h2><h3 id="_3-1-身份凭证-credentials" tabindex="-1">3.1 身份凭证 (Credentials) <a class="header-anchor" href="#_3-1-身份凭证-credentials" aria-label="Permalink to &quot;3.1 身份凭证 (Credentials)&quot;">​</a></h3><p>默认情况下，跨域的 CORS 请求是<strong>完全匿名</strong>的，浏览器发起请求时绝对不会自动携带目标域名的 Cookie 或 HTTP 认证信息。要打通这层壁垒，需要前后端双向配置：</p><ul><li><p><strong>防线一：前端显式声明开启凭证开关</strong>。</p><ul><li>Fetch API: 设置 <code>credentials: &#39;include&#39;</code>。</li><li>Axios / XMLHttpRequest: 设置 <code>withCredentials: true</code>。</li></ul></li><li><p><strong>防线二：后端显式返回放行许可头</strong>。</p><ul><li>服务端响应中必须包含 <code>Access-Control-Allow-Credentials: true</code>。</li></ul></li><li><p><strong>警告</strong>：一旦开启了身份凭证（<code>Allow-Credentials: true</code>），后端的 <code>Access-Control-Allow-Origin</code> <strong>绝对不能</strong>设置为通配符 <code>*</code>。服务端必须动态读取请求头中的 <code>Origin</code> 字段，并将其原封不动地反射回响应头中，否则浏览器会直接报错拦截。</p></li></ul><h3 id="_3-2-预检缓存-access-control-max-age" tabindex="-1">3.2 预检缓存 (Access-Control-Max-Age) <a class="header-anchor" href="#_3-2-预检缓存-access-control-max-age" aria-label="Permalink to &quot;3.2 预检缓存 (Access-Control-Max-Age)&quot;">​</a></h3><ul><li><strong>核心痛点</strong>：每次发送 JSON 数据的 API 请求都要经历 <code>OPTIONS + POST</code> 两次网络往返，这不仅让系统的网络请求数量瞬间翻倍，更极大延长了前端的页面网络延迟（TTFB），严重消耗服务器吞吐量。</li><li><strong>优化手段</strong>：服务器在 <code>OPTIONS</code> 的成功响应头中，加上 <code>Access-Control-Max-Age: &lt;秒数&gt;</code>（例如 <code>86400</code>，代表 24 小时）。</li><li><strong>优化效果</strong>：浏览器会在指定的秒数内“<strong>记住</strong>”这次审批结果。在缓存有效期内，相同特征的复杂跨域请求将直接绕过 OPTIONS 探测阶段，变回一次网络往返的丝滑体验。</li></ul><h2 id="_4-cors-深度对比及常见问题-faq" tabindex="-1">4. CORS 深度对比及常见问题 (FAQ) <a class="header-anchor" href="#_4-cors-深度对比及常见问题-faq" aria-label="Permalink to &quot;4. CORS 深度对比及常见问题 (FAQ)&quot;">​</a></h2><h3 id="_4-1-简单请求-vs-预检请求-核心差异对比" tabindex="-1">4.1 简单请求 vs 预检请求 核心差异对比 <a class="header-anchor" href="#_4-1-简单请求-vs-预检请求-核心差异对比" aria-label="Permalink to &quot;4.1 简单请求 vs 预检请求 核心差异对比&quot;">​</a></h3><table tabindex="0"><thead><tr><th>对比维度</th><th>简单请求 (Simple Request)</th><th>预检请求 (Preflight Request)</th></tr></thead><tbody><tr><td><strong>请求网络次数</strong></td><td>1 次（直接发送真实的业务请求）。</td><td>2 次（1 次 OPTIONS 预检探测 + 1 次真实业务请求）。</td></tr><tr><td><strong>典型触发场景</strong></td><td>传统的 HTML <code>&lt;form&gt;</code> 表单提交、纯文本 POST 请求。</td><td>现代前端开发中使用 Axios 发送 JSON 数据、请求头携带 JWT Token、使用 <code>PUT</code>/<code>DELETE</code> 执行 RESTful 操作。</td></tr><tr><td><strong>安全拦截时机</strong></td><td><strong>事后拦截</strong>。请求已经抵达服务器并被执行，只是浏览器拒绝将返回的结果交给前端 JS。</td><td><strong>事前拦截</strong>。如果 OPTIONS 预检失败，真正的业务请求根本不会被发出，从源头上保护了服务器数据。</td></tr></tbody></table><h3 id="_4-2-为什么我在前端配置了代理-proxy-之后-就不存在跨域问题了" tabindex="-1">4.2 为什么我在前端配置了代理 (Proxy) 之后，就不存在跨域问题了？ <a class="header-anchor" href="#_4-2-为什么我在前端配置了代理-proxy-之后-就不存在跨域问题了" aria-label="Permalink to &quot;4.2 为什么我在前端配置了代理 (Proxy) 之后，就不存在跨域问题了？&quot;">​</a></h3><ul><li><strong>根本原因</strong>：跨域同源策略（Same-Origin Policy）是<strong>浏览器</strong>独有的安全限制。</li><li><strong>运行机制</strong>：在开发阶段，由 Vite 或 Webpack 在本地启动了一个同源的 Node.js 中间件服务器。前端的代码直接请求这个本地服务器（同源，不触发 CORS）。本地服务器收到请求后，在后台通过纯代码将请求转发给真正的后端目标服务器。因为<strong>服务器与服务器之间的通信不受浏览器同源策略限制</strong>，因此完美绕过了跨域报错。</li></ul><h3 id="_4-3-cors-跨域报错-到底是前端的锅还是后端的锅" tabindex="-1">4.3 CORS 跨域报错，到底是前端的锅还是后端的锅？ <a class="header-anchor" href="#_4-3-cors-跨域报错-到底是前端的锅还是后端的锅" aria-label="Permalink to &quot;4.3 CORS 跨域报错，到底是前端的锅还是后端的锅？&quot;">​</a></h3><ul><li><strong>99% 的情况下，是后端（或运维网关）的锅。</strong></li><li>跨域的本质是浏览器代替前端去询问服务器“<strong>是否愿意接受外来域名的访问</strong>”。前端除了在需要携带 Cookie 时配置一下 <code>withCredentials</code> 外，没有权限改变任何 HTTP 响应头。真正决定跨域能否成功的许可权（即返回各类 <code>Access-Control-Allow-*</code> 响应头），完全掌握在服务端的 Nginx 配置、网关或后端的 CORS 中间件手里。</li></ul><h3 id="_4-4-为什么抓包看到后端明明返回了-200-但前端控制台依然报-cors-错误" tabindex="-1">4.4 为什么抓包看到后端明明返回了 200，但前端控制台依然报 CORS 错误？ <a class="header-anchor" href="#_4-4-为什么抓包看到后端明明返回了-200-但前端控制台依然报-cors-错误" aria-label="Permalink to &quot;4.4 为什么抓包看到后端明明返回了 200，但前端控制台依然报 CORS 错误？&quot;">​</a></h3><ul><li><strong>场景 A（简单请求）</strong>：后端虽然业务逻辑执行成功并成功返回了状态码 200，但其响应头里缺失了 <code>Access-Control-Allow-Origin</code> 字段，被浏览器在最后一关无情拦截。</li><li><strong>场景 B（预检请求）</strong>：后端没有针对 <code>OPTIONS</code> 方法做专门的路由放行或中间件拦截。虽然业务路由对 <code>POST</code> 正常响应 200，但面对前置的 <code>OPTIONS</code> 请求时，后端可能报错或者返回了没有跨域头的 200，导致预检死在第一步，真实的 <code>POST</code> 请求被浏览器直接扼杀。</li></ul><h3 id="_4-5-为什么现在的跨域请求中-几乎都不带-withcredentials-参数了" tabindex="-1">4.5 为什么现在的跨域请求中，几乎都不带 withCredentials 参数了？ <a class="header-anchor" href="#_4-5-为什么现在的跨域请求中-几乎都不带-withcredentials-参数了" aria-label="Permalink to &quot;4.5 为什么现在的跨域请求中，几乎都不带 withCredentials 参数了？&quot;">​</a></h3><ul><li><strong>因为现代前后端架构的主流鉴权范式，已经从“依赖浏览器自动发送 Cookie” 全面转向了“前端手动携带 JWT (Token)”。</strong></li><li>过去使用 Session/Cookie 鉴权时，必须开启 <code>withCredentials</code> 来强制浏览器在跨域时带上身份凭证。但在现代微服务架构中，前端通常会将登录后获取的 JWT 存储在 <code>localStorage</code> 中，并在发送请求时通过 Axios 拦截器<strong>手动</strong>将其塞入请求头（如 <code>Authorization: Bearer &lt;token&gt;</code>）。既然身份信息是前端用 JS 代码主动塞进 HTTP Header 的，不再依赖浏览器底层的 Cookie 自动发送机制，<code>withCredentials</code> 自然就失去了存在的意义。此外，抛弃 Cookie 转向手动携带 Token 的做法，还能从物理层面上彻底免疫 CSRF（跨站请求伪造）攻击。</li></ul></div>`,
  )
}
const a = r.setup
r.setup = (e, o) => {
  const t = i()
  return (
    (t.modules || (t.modules = new Set())).add('webSecurity/cors.md'),
    a ? a(e, o) : void 0
  )
}
const O = c(r, [['ssrRender', h]])
export { T as __pageData, O as default }
//# sourceMappingURL=webSecurity_cors.md.js.map
