import { ssrRenderAttrs as i } from 'vue/server-renderer'
import { useSSRContext as r } from 'vue'
import { _ as s } from './plugin-vue_export-helper.CTtO9zSR.js'
;(function () {
  try {
    var o =
      typeof window < 'u'
        ? window
        : typeof global < 'u'
          ? global
          : typeof globalThis < 'u'
            ? globalThis
            : typeof self < 'u'
              ? self
              : {}
    o.SENTRY_RELEASE = { id: '63d551497be1158ac55eeefaf1dec4a9182e3c76' }
    var t = new o.Error().stack
    t &&
      ((o._sentryDebugIds = o._sentryDebugIds || {}),
      (o._sentryDebugIds[t] = '0778f90c-765c-4cf3-b903-57541dec3dc7'),
      (o._sentryDebugIdIdentifier =
        'sentry-dbid-0778f90c-765c-4cf3-b903-57541dec3dc7'))
  } catch {}
})()
const m = JSON.parse(
    '{"title":"文档流","description":"","frontmatter":{},"headers":[],"relativePath":"css/basic/documentFlow.md","filePath":"css/basic/documentFlow.md","lastUpdated":1782463918000}',
  ),
  d = { name: 'css/basic/documentFlow.md' }
function a(o, t, e, l, c, g, p, h) {
  t(
    `<div${i(l)}><h1 id="文档流" tabindex="-1">文档流 <a class="header-anchor" href="#文档流" aria-label="Permalink to &quot;文档流&quot;">​</a></h1><p>文档流是浏览器默认的排版引擎。理解文档流的物理规则，是解决所有 CSS 布局 Bug 的前提。当你觉得“CSS 不听话”时，通常是因为你不小心破坏了文档流，或者不理解它的隐形规则（如外边距合并、行高机制等）。</p><h2 id="_1-布局基础-文档流与-display-详解" tabindex="-1">1. 布局基础：文档流与 Display 详解 <a class="header-anchor" href="#_1-布局基础-文档流与-display-详解" aria-label="Permalink to &quot;1. 布局基础：文档流与 Display 详解&quot;">​</a></h2><p>理解文档流和 display 属性是掌握 CSS 布局的基石。</p><h3 id="_1-1-什么是-标准文档流-normal-document-flow" tabindex="-1">1.1 什么是“标准文档流” (Normal Document Flow)? <a class="header-anchor" href="#_1-1-什么是-标准文档流-normal-document-flow" aria-label="Permalink to &quot;1.1 什么是“标准文档流” (Normal Document Flow)?&quot;">​</a></h3><p>标准文档流（Normal Flow）是浏览器默认排列 HTML 元素的规则。如果你不写任何 CSS 布局代码（如 <code>float</code>, <code>position</code>, <code>flex</code>, <code>grid</code>），元素就会按照文档流进行排列。</p><p><strong>核心规则：</strong></p><ul><li><strong>从上到下</strong>：块级元素（Block）会在垂直方向上一个接一个地排列。</li><li><strong>从左到右</strong>：行内元素（Inline）会在水平方向上从左向右排列，直到一行排满，自动换行到下一行。</li></ul><h3 id="_1-2-display-属性的核心模式" tabindex="-1">1.2 Display 属性的核心模式 <a class="header-anchor" href="#_1-2-display-属性的核心模式" aria-label="Permalink to &quot;1.2 Display 属性的核心模式&quot;">​</a></h3><p><code>display</code> 属性决定了元素在文档流中的行为方式（它是“<strong>块</strong>”还是“<strong>行内</strong>”？），以及它的子元素如何布局。</p><h4 id="_1-2-1-display-block-块级元素" tabindex="-1">1.2.1 <code>display: block</code> (块级元素) <a class="header-anchor" href="#_1-2-1-display-block-块级元素" aria-label="Permalink to &quot;1.2.1 \`display: block\` (块级元素)&quot;">​</a></h4><p>块级元素是页面结构的骨架。</p><ul><li><strong>默认元素</strong>：<code>&lt;div&gt;</code>, <code>&lt;p&gt;</code>, <code>&lt;h1&gt;</code>~<code>&lt;h6&gt;</code>, <code>&lt;ul&gt;</code>, <code>&lt;li&gt;</code>, <code>&lt;section&gt;</code>, <code>&lt;article&gt;</code> 等。</li><li><strong>行为特征</strong>： <ul><li><strong>独占一行</strong>：无论内容多少，它总是强制换行，左右两边不允许有其他元素（除非使用浮动或 Flex）。</li><li><strong>填满容器</strong>：默认宽度（<code>width</code>）自动填满父容器的 100%。</li><li><strong>支持盒模型</strong>：你可以自由设置 <code>width</code>, <code>height</code>, <code>margin</code>, <code>padding</code>，它们都会生效并占据空间。</li></ul></li></ul><h4 id="_1-2-2-display-inline-行内元素" tabindex="-1">1.2.2 <code>display: inline</code> (行内元素) <a class="header-anchor" href="#_1-2-2-display-inline-行内元素" aria-label="Permalink to &quot;1.2.2 \`display: inline\` (行内元素)&quot;">​</a></h4><p>行内元素主要用于包裹文本内容。</p><ul><li><strong>默认元素</strong>：<code>&lt;span&gt;</code>, <code>&lt;a&gt;</code>, <code>&lt;em&gt;</code>, <code>&lt;strong&gt;</code>, <code>&lt;label&gt;</code> 等。</li><li><strong>行为特征</strong>： <ul><li><strong>并排显示</strong>：和其他行内元素在同一行显示，不会自动换行。</li><li><strong>宽度随内容</strong>：宽度仅由其包含的内容决定，<strong>无法</strong>手动设置 <code>width</code> 和 <code>height</code>（设置了也无效）。</li><li><strong>盒模型受限</strong>：</li></ul></li><li><strong>水平方向</strong>：<code>margin-left</code>, <code>margin-right</code>, <code>padding-left</code>, <code>padding-right</code> <strong>有效</strong>，会推开其他元素。</li><li><strong>垂直方向</strong>：<code>margin-top</code>, <code>margin-bottom</code> <strong>无效</strong>。<code>padding-top</code>, <code>padding-bottom</code> 视觉上会有背景色延伸，但<strong>不会推开</strong>上下方的其他元素（不会影响文档流的高度）。</li></ul><h4 id="_1-2-3-display-inline-block-行内块元素" tabindex="-1">1.2.3 <code>display: inline-block</code> (行内块元素) <a class="header-anchor" href="#_1-2-3-display-inline-block-行内块元素" aria-label="Permalink to &quot;1.2.3 \`display: inline-block\` (行内块元素)&quot;">​</a></h4><p>这是结合了前两者优点的混合模式。在 Flexbox 流行之前，常用于制作导航栏或并排排列的卡片。</p><ul><li><strong>默认元素</strong>：<code>&lt;img&gt;</code>, <code>&lt;input&gt;</code>, <code>&lt;button&gt;</code>, <code>&lt;textarea&gt;</code>。</li><li><strong>行为特征</strong>： <ul><li><strong>对外像 Inline</strong>：可以和其他元素并排在一行。</li><li><strong>对内像 Block</strong>：可以完整支持盒模型，设置 <code>width</code>, <code>height</code>, <code>margin</code>, <code>padding</code> 均有效。</li></ul></li></ul><h4 id="_1-2-4-display-none-vs-visibility-hidden" tabindex="-1">1.2.4 <code>display: none</code> vs <code>visibility: hidden</code> <a class="header-anchor" href="#_1-2-4-display-none-vs-visibility-hidden" aria-label="Permalink to &quot;1.2.4 \`display: none\` vs \`visibility: hidden\`&quot;">​</a></h4><p>虽然它们都用于隐藏元素，但对文档流的影响完全不同。</p><table tabindex="0"><thead><tr><th>属性</th><th>描述</th><th>对文档流的影响</th><th>重绘/回流</th></tr></thead><tbody><tr><td><strong><code>display: none</code></strong></td><td><strong>完全移除</strong></td><td>元素从文档流中消失，<strong>不占据任何空间</strong>。就像它从未存在过一样。</td><td>触发<strong>回流 (Reflow)</strong> 和重绘。</td></tr><tr><td><strong><code>visibility: hidden</code></strong></td><td><strong>视觉隐藏</strong></td><td>元素不可见，但在文档流中<strong>依然占据原来的空间</strong>（变成透明的）。</td><td>只触发<strong>重绘 (Repaint)</strong>。</td></tr></tbody></table><h2 id="bfc" tabindex="-1">2. BFC (块级格式化上下文) <a class="header-anchor" href="#bfc" aria-label="Permalink to &quot;2. BFC (块级格式化上下文){#bfc}&quot;">​</a></h2><p>这是理解文档流最重要的高级概念。</p><p><strong>定义</strong>：<strong>BFC (Block Formatting Context)</strong> 是页面上的一个独立的渲染区域。</p><p><strong>形象理解</strong>：BFC 就像是一个<strong>密封的箱子</strong>。</p><ul><li>箱子里的元素怎么乱跑（比如浮动），都不会影响到箱子外面的元素。</li><li>箱子外面的元素（比如 Margin），也穿透不进箱子内部。</li></ul><h3 id="_2-1-如何触发-bfc" tabindex="-1">2.1 如何触发 BFC？ <a class="header-anchor" href="#_2-1-如何触发-bfc" aria-label="Permalink to &quot;2.1 如何触发 BFC？&quot;">​</a></h3><p>只要给元素添加以下任意一个属性，就会创建 BFC：</p><ul><li><code>overflow</code>: <code>hidden</code>、<code>auto</code> 或 <code>scroll</code> (最常用的方式)。</li><li><code>display</code>: <code>flex</code>、<code>grid</code>、<code>inline-block</code> 或 <code>flow-root</code> (现代标准推荐)。</li><li><code>position</code>: <code>absolute</code> 或 <code>fixed</code>。</li><li><code>float</code>: <code>left</code> 或 <code>right</code>。</li></ul><h3 id="_2-2-bfc-的三大-超能力-用途" tabindex="-1">2.2 BFC 的三大“超能力” (用途) <a class="header-anchor" href="#_2-2-bfc-的三大-超能力-用途" aria-label="Permalink to &quot;2.2 BFC 的三大“超能力” (用途)&quot;">​</a></h3><ul><li><strong>包含浮动元素</strong> (解决高度塌陷)。</li><li><strong>阻止外边距折叠</strong> (解决 Margin Collapse)。</li><li><strong>防止被浮动元素覆盖</strong> (两栏自适应布局)。</li></ul><h2 id="ifc" tabindex="-1">3. 核心概念：IFC (行内格式化上下文) <a class="header-anchor" href="#ifc" aria-label="Permalink to &quot;3. 核心概念：IFC (行内格式化上下文){#ifc}&quot;">​</a></h2><p>如果说 BFC 是管理“<strong>块</strong>”的密封箱子，那么 <strong>IFC (Inline Formatting Context)</strong> 就是管理“<strong>文字和行内元素</strong>”的<strong>流水排版系统</strong>。</p><p><strong>定义</strong>：IFC 规定了行内级别盒子（如文字、<code>&lt;span&gt;</code>、<code>&lt;img&gt;</code>、<code>inline-block</code>）在容器中是如何进行水平和垂直排布的。</p><p><strong>形象理解</strong>：IFC 就像老式打字机打字。字（元素）一个接一个从左向右紧密排列，碰到边界了，打字机“叮”一声换到下一行继续排。</p><h3 id="_3-1-ifc-的内部运作机制-行框与基线" tabindex="-1">3.1 IFC 的内部运作机制（行框与基线） <a class="header-anchor" href="#_3-1-ifc-的内部运作机制-行框与基线" aria-label="Permalink to &quot;3.1 IFC 的内部运作机制（行框与基线）&quot;">​</a></h3><p>在 IFC 中，有几个看不见的“幽灵结构”在控制着布局：</p><ol><li><p><strong>行框 (Line Box)</strong>：</p><ul><li>IFC 中的每一行文字/元素都会生成一个看不见的矩形框，叫作“行框”。</li><li>行框的<strong>宽度</strong>由父容器决定（并受浮动元素挤压而变窄）。</li><li>行框的<strong>高度</strong>由这一行里面<strong>最高的那个元素</strong>（或是文字的行高 <code>line-height</code>）决定。</li></ul></li><li><p><strong>水平对齐 (<code>text-align</code>)</strong>：</p><ul><li>当一行排不满时，行内盒子在行框里的水平位置由父容器的 <code>text-align</code> 属性控制（<code>left</code>, <code>center</code>, <code>right</code>, <code>justify</code>）。</li></ul></li><li><p><strong>垂直对齐 (<code>vertical-align</code> 与 Baseline)</strong>：</p><ul><li>在同一个行框里，高矮胖瘦不同的元素（比如大号字、小号字、图片）默认是按照<strong>基线 (Baseline)</strong> 对齐的，而不是底部对齐。</li><li>你可以使用 <code>vertical-align</code> (<code>top</code>, <code>middle</code>, <code>bottom</code>, <code>baseline</code>) 来改变它们在行框内的上下位置。</li></ul></li></ol><h2 id="_4-文档流中的常见-怪异-问题与底层原因" tabindex="-1">4. 文档流中的常见“怪异”问题与底层原因 <a class="header-anchor" href="#_4-文档流中的常见-怪异-问题与底层原因" aria-label="Permalink to &quot;4. 文档流中的常见“怪异”问题与底层原因&quot;">​</a></h2><p>以下是开发者经常遇到的布局“灵异现象”，它们都源于 BFC 或 IFC 的底层渲染机制。</p><h3 id="_4-1-外边距合并-margin-collapse-——-bfc-相关" tabindex="-1">4.1 外边距合并 (Margin Collapse) —— [BFC 相关] <a class="header-anchor" href="#_4-1-外边距合并-margin-collapse-——-bfc-相关" aria-label="Permalink to &quot;4.1 外边距合并 (Margin Collapse) —— [BFC 相关]&quot;">​</a></h3><p><strong>现象</strong>：</p><ul><li><strong>兄弟之间</strong>：上下相邻的两个 div，分别有 <code>margin-bottom: 30px</code> 和 <code>margin-top: 20px</code>，最终间距只有 <strong>30px</strong> (取最大值)。</li><li><strong>父子之间</strong>：给子元素设置 <code>margin-top: 20px</code>，子元素没相对于父元素下移，而是<strong>带着父元素一起掉下来了</strong>。</li></ul><p><strong>解决方案</strong>：</p><table tabindex="0"><thead><tr><th>场景</th><th>解决方案</th><th>备注</th></tr></thead><tbody><tr><td><strong>兄弟合并</strong></td><td>无法通过自身属性消除</td><td>通常不解决，接受这个设定，或者只给一个方向设 Margin。</td></tr><tr><td><strong>父子合并</strong> (最常见)</td><td>1. <strong>给父元素触发 BFC</strong> (如 <code>overflow: hidden</code> 或 <code>display: flow-root</code>) <br>2. <strong>给父元素加边框/Padding</strong> (如 <code>padding-top: 1px</code>)</td><td>只要父子之间有“<strong>阻隔</strong>”(边框/padding) 或父元素是“<strong>密封箱子</strong>”(BFC)，合并就会取消。</td></tr></tbody></table><h3 id="_4-2-inline-block-元素之间的-幽灵空白-——-ifc-相关" tabindex="-1">4.2 Inline-Block 元素之间的“幽灵空白” —— [IFC 相关] <a class="header-anchor" href="#_4-2-inline-block-元素之间的-幽灵空白-——-ifc-相关" aria-label="Permalink to &quot;4.2 Inline-Block 元素之间的“幽灵空白” —— [IFC 相关]&quot;">​</a></h3><p><strong>现象</strong>： 当你把几个 <code>display: inline-block</code> 的元素横向排列时，即使 margin 设为 0，它们之间也总有 <strong>4px 左右的间隙</strong>。</p><p><strong>原因</strong>： 因为它们参与了 <strong>IFC 排版</strong>。浏览器把 HTML 代码中的<strong>换行符</strong>（回车）当成了一个普通的“空格字符”渲染到了行框（Line Box）里。</p><p><strong>解决方案</strong>：</p><ul><li><strong>方案 A (推荐)</strong>：改用 <strong>Flexbox</strong> 布局（彻底脱离 IFC）。</li><li><strong>方案 B (Hack)</strong>：父元素 <code>font-size: 0</code>，消除空格字符的大小，子元素再恢复字体大小。</li></ul><h3 id="_4-3-图片底部多出的-下巴-3px-空隙-——-ifc-相关" tabindex="-1">4.3 图片底部多出的“下巴” (3px 空隙) —— [IFC 相关] <a class="header-anchor" href="#_4-3-图片底部多出的-下巴-3px-空隙-——-ifc-相关" aria-label="Permalink to &quot;4.3 图片底部多出的“下巴” (3px 空隙) —— [IFC 相关]&quot;">​</a></h3><p><strong>现象</strong>： 在一个 div 里放一张 img，div 的高度会比 img 高出约 3-5px，导致底部有缝隙。</p><p><strong>原因</strong>： 图片默认是 <code>inline</code> 元素，处于 <strong>IFC 规则</strong>中。IFC 默认按照<strong>基线 (Baseline)</strong> 对齐，而基线下方必须为英文字母（如 <code>g</code>, <code>y</code>, <code>p</code>）的“<strong>尾巴</strong>”留出空间。图片坐在了基线上，下方预留的空白就成了那个“<strong>下巴</strong>”。</p><p><strong>解决方案</strong>：</p><ul><li><strong>方案 A (推荐)</strong>：将图片脱离 IFC，设为块级。<code>img { display: block; }</code></li><li><strong>方案 B</strong>：改变 IFC 中的对齐方式。<code>img { vertical-align: bottom; }</code></li></ul><h3 id="_4-4-高度塌陷-height-collapse-——-bfc-相关" tabindex="-1">4.4 高度塌陷 (Height Collapse) —— [BFC 相关] <a class="header-anchor" href="#_4-4-高度塌陷-height-collapse-——-bfc-相关" aria-label="Permalink to &quot;4.4 高度塌陷 (Height Collapse) —— [BFC 相关]&quot;">​</a></h3><p><strong>现象</strong>： 父容器没有设置高度，里面的子元素全部 <code>float: left</code>。结果父容器<strong>高度变成了 0</strong>。</p><p><strong>原因</strong>： 浮动元素脱离了标准文档流。父容器在计算高度时，忽略了浮动的子元素。</p><p><strong>解决方案</strong>：</p><ul><li><strong>现代方案</strong>：给父元素触发 BFC。<code>display: flow-root;</code></li><li><strong>经典 Clearfix</strong>：使用伪元素闭合浮动。</li></ul><h3 id="_4-5-单行文字的垂直居中黑魔法-——-ifc-相关" tabindex="-1">4.5 单行文字的垂直居中黑魔法 —— [IFC 相关] <a class="header-anchor" href="#_4-5-单行文字的垂直居中黑魔法-——-ifc-相关" aria-label="Permalink to &quot;4.5 单行文字的垂直居中黑魔法 —— [IFC 相关]&quot;">​</a></h3><p><strong>现象</strong>： 将元素的 <code>line-height</code> 设置得和 <code>height</code> 一样高，文字就会自动垂直居中。</p><p><strong>原因</strong>： 这也是 <strong>IFC</strong> 的杰作。当你设置了 <code>line-height: 50px</code>，相当于强制把这一行的“<strong>行框 (Line Box)</strong>”撑到了 50px 高。在行框内部，文字默认是基于中轴线对称分布的，所以在视觉上就完美居中了。</p><h2 id="_5-总结-如何避免-90-的文档流问题" tabindex="-1">5. 总结：如何避免 90% 的文档流问题？ <a class="header-anchor" href="#_5-总结-如何避免-90-的文档流问题" aria-label="Permalink to &quot;5. 总结：如何避免 90% 的文档流问题？&quot;">​</a></h2><ul><li><strong>拥抱现代布局</strong>：能用 Flex/Grid 就别用 Float/Inline-Block。现代布局系统规避了几乎所有传统 BFC/IFC 的坑。</li><li><strong>理解盒模型</strong>：始终使用 <code>box-sizing: border-box</code>。</li><li><strong>警惕 Margin 合并</strong>：当父子元素边缘重合时，优先想到 BFC 或使用 Padding 代替 Margin。</li><li><strong>图片初始化</strong>：习惯性把布局用的图片设为 <code>display: block</code>，彻底告别 IFC 带来的神秘间隙。</li></ul></div>`,
  )
}
const n = d.setup
d.setup = (o, t) => {
  const e = r()
  return (
    (e.modules || (e.modules = new Set())).add('css/basic/documentFlow.md'),
    n ? n(o, t) : void 0
  )
}
const x = s(d, [['ssrRender', a]])
export { m as __pageData, x as default }
//# sourceMappingURL=css_basic_documentFlow.md.js.map
