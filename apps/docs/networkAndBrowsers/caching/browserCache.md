# 浏览器缓存机制

> 相关：[前端缓存策略](/networkAndBrowsers/caching/frontendCacheStrategy) 从分层架构视角看待缓存；[资源加载优化](/performanceOptimization/resourceLoading) 结合预加载与缓存命中。本文聚焦 HTTP 强缓存与协商缓存。

## 1. 什么是浏览器缓存？为什么要缓存？

**浏览器缓存（Browser Caching）** 是指浏览器在本地磁盘或内存中保存用户最近请求过的网络资源（如 HTML、CSS、JavaScript 文件、图片等），当用户再次访问该资源时，浏览器可以直接从本地读取，而无需再次向服务器发起网络请求。

**核心优势：**

- **减少网络延迟，加快页面打开速度**（最核心的用户体验提升）。
- **减少服务器的压力**，节省服务器带宽。
- **在网络极差或断网状态下，依然能提供基础的页面访问**。

## 2. 缓存的存放位置

按读取优先级从高到低，浏览器缓存可以分为以下四个位置：

- **Service Worker：** 运行在浏览器背后的独立线程，常用于实现离线缓存（PWA 技术）。它可以自由控制缓存哪些文件、如何匹配缓存等，灵活性最高。它是四层里唯一能**完全接管请求**的一层，详见 [PWA 与 Service Worker 缓存](/networkAndBrowsers/caching/serviceWorkerPwa)。
- **Memory Cache（内存缓存）：** 包含当前页面中已经抓取到的资源（如页面上下载的样式、脚本、图片）。读取速度极快，但**存活时间短**，一旦关闭 Tab 页，内存缓存就会被释放。
- **Disk Cache（磁盘缓存）：** 存储在硬盘中的缓存。读取速度稍慢，但**容量大、存储时间长**。绝大部分的缓存都来自 Disk Cache。
- **Push Cache（推送缓存）：** HTTP/2 中的新特性。只在会话（Session）中存在，一旦会话结束就被释放。

## 3. 强缓存 (Strong Cache)

强缓存是最高效的缓存方式。当浏览器判断命中强缓存时，**完全不会向服务器发送任何网络请求**，而是直接从本地（Memory Cache 或 Disk Cache）读取资源。

在浏览器的 Network 面板中，你会看到状态码为 `200 OK (from memory cache)` 或 `200 OK (from disk cache)`。

强缓存通过 HTTP 响应头中的两个字段来控制：`Expires` 和 `Cache-Control`。

### 3.1 Expires (HTTP/1.0 产物)

- **作用**：服务器返回一个**绝对的过期时间**（GMT 格式的时间戳）。在此时刻之前，浏览器都会直接使用本地缓存。
- **示例**：`Expires: Wed, 21 Oct 2026 07:28:00 GMT`
- **致命缺陷**：它依赖于客户端（用户电脑）的系统时间。如果用户的本地时间与服务器时间不一致，或者用户手动修改了本地时间，那么缓存的判断就会完全失效。因此，它现在基本上被淘汰了，仅作为向后兼容的保留手段。

### 3.2 Cache-Control (HTTP/1.1 标准，目前的主流)

- **作用**：为了克服 `Expires` 的缺陷，`Cache-Control` 使用**相对时间**来控制缓存。
- **最高优先级**：如果同时存在 `Expires` 和 `Cache-Control`，浏览器会**优先遵循 `Cache-Control`**。
- **常见指令**：
  - **`max-age=<seconds>`**：最重要的指令。表示资源从被请求的那一刻起，在指定的秒数内有效。例如 `Cache-Control: max-age=31536000` 表示缓存一年。在这期间内，直接走强缓存。
  - **`no-cache`**：它的意思不是“**不缓存**”，而是“**可以缓存，但在每次使用前，必须向服务器发送请求，进行协商缓存的验证**”。
  - **`no-store`**：这才是真正的“**绝对不缓存**”。浏览器和任何中间代理都不允许存储这个响应的任何内容。每次必须向服务器发完整请求。
  - **`public`**：表示该响应可以被任何对象（包括客户端浏览器、CDN 节点、代理服务器等）缓存。
  - **`private`**：表示该响应是针对单个用户的，只能被客户端浏览器缓存，中间的代理服务器（CDN）不能缓存。这是默认值。

### 3.3 `Cache-Control` 指令全景

[width(9,26,34,31)]

| 分组         | 指令                         | 含义                                                         | 典型用法                                           |
| :----------- | :--------------------------- | :----------------------------------------------------------- | :------------------------------------------------- |
| **缓存位置** | `public`                     | 允许浏览器、CDN、代理等**任何环节**缓存这份响应。            | 公开分发的静态资源。                               |
|              | `private`                    | 只允许**终端浏览器**缓存，CDN / 代理不许存（默认语义）。     | 登录后的个性化接口。                               |
| **有效期**   | `max-age=<s>`                | 客户端缓存的有效秒数（相对时间）。                           | 见上文。                                           |
|              | `s-maxage=<s>`               | **只对共享缓存（CDN / 代理）生效**，且优先级高于 `max-age`。 | 想让 CDN 比浏览器更早回源。                        |
|              | `immutable`                  | 有效期内**连用户刷新都不回源**，把 304 也省掉。              | 带 Hash 的 JS / CSS / 字体。                       |
| **过期校验** | `must-revalidate`            | 一旦过期就**必须**回源验证，禁止拿过期缓存兜底。             | 准确性优先的数据接口。                             |
|              | `proxy-revalidate`           | 同上，但只约束共享缓存。                                     | 配合 CDN 的接口。                                  |
|              | `stale-while-revalidate=<s>` | 过期后的一段窗口内，**先返回旧内容**、后台静默更新。         | 允许短暂过期的配置 / 列表接口。                    |
|              | `stale-if-error=<s>`         | 回源失败时，在窗口内**用旧缓存兜底**。                       | 弱网与容灾兜底。                                   |
| **禁止缓存** | `no-cache`                   | 可以缓存，但**每次使用前必须验证**。                         | HTML 入口。                                        |
|              | `no-store`                   | 任何环节都不许落盘。                                         | 支付、流水等敏感响应。                             |
| **其他**     | `no-transform`               | 禁止代理压缩、转码（例如替你“**优化**”图片）。               | 已经压好的图片 / 视频。                            |
|              | `max-age=0`                  | 缓存立刻过期，每次都得回源验证。                             | 语义上偏「立即过期」，与 `no-cache` 的区别见 FAQ。 |

### 3.4 启发式缓存

> [!WARNING] 这是线上最常见的“缓存幽灵”
> 响应里既没有 `Cache-Control` 也没有 `Expires` 时，缓存**不会立刻失效**。浏览器会启用**启发式缓存（Heuristic Caching）**：把响应头 `Date` 减去 `Last-Modified` 的差值取 **10%**，当作这份缓存的有效期。

也就是说，一个 `Last-Modified` 在 100 天前的文件，会被**自动强缓存约 10 天**；这期间你改了后端逻辑，用户依然拿着旧响应，直到缓存自然过期。所以结论很直接：

- **想“不缓存”的响应，必须显式写 `Cache-Control: no-cache` 或 `no-store`，不能靠“什么都不写”**。
- 连 `Last-Modified` 都没有时，部分浏览器还会退回按 `Date` 起算的默认值，行为更加不可控。

### 3.5 常见资源的推荐组合

[width(27,43,30)]

| 资源类型               | 推荐响应头                                                      | 说明                                     |
| :--------------------- | :-------------------------------------------------------------- | :--------------------------------------- |
| 带 Hash 的构建产物     | `Cache-Control: public, max-age=31536000, immutable`            | 内容变 URL 就变，可以放心长缓存。        |
| 入口 HTML              | `Cache-Control: no-cache`                                       | 每次协商验证，保证拿到新版本的资源引用。 |
| 需要登录的 JSON 接口   | `Cache-Control: private, no-cache`                              | 不许进 CDN，且每次都校验。               |
| 公开的配置 / 枚举接口  | `Cache-Control: public, max-age=300, stale-while-revalidate=60` | CDN 兜住流量，过期也能先渲染。           |
| 可变 URL 的图片 / 媒体 | `Cache-Control: public, max-age=86400, no-transform`            | 短缓存 + 禁止代理转码。                  |
| 支付、流水、验证码     | `Cache-Control: no-store`                                       | 任何环节都不落盘。                       |

## 4. 协商缓存 (Negotiation Cache)

当**强缓存失效**（即没有设置强缓存，或者 `max-age` 已过期，或者设置了 `no-cache`），浏览器就会向服务器发起请求，**验证本地缓存的内容是否仍然是最新版本**。这个验证过程就是协商缓存。

协商缓存需要客户端和服务器互相配合，通过两对特殊的 HTTP 头部字段来实现。

### 4.1 Last-Modified / If-Modified-Since (基于修改时间)

1.  **初次请求**：服务器在返回资源时，会在响应头中加上 `Last-Modified`，表示这个资源在服务器上的**最后修改时间**。
    - `Last-Modified: Fri, 12 Feb 2026 15:00:00 GMT`
2.  **再次请求**：当缓存过期，浏览器再次请求该资源时，会在请求头中自动加上 `If-Modified-Since`，其值就是上次收到的 `Last-Modified` 的值。
3.  **服务器比对**：服务器收到请求后，拿 `If-Modified-Since` 的时间与该资源在服务器上当前的最后修改时间进行比对。
    - **如果没有修改**：服务器返回状态码 **`304 Not Modified`**，且不返回资源正文（Body 为空）。这告诉浏览器：“资源没变，你继续用你本地的旧缓存吧。”（这就是协商缓存命中的表现）。
    - **如果已修改**：服务器返回状态码 `200 OK`，并带上最新的资源正文和新的 `Last-Modified` 时间。

- **缺陷**：
  - `Last-Modified` 的时间精度只能精确到**秒**。如果一个文件在 1 秒内被修改了多次，它无法察觉。
  - 如果某些文件被定时打开并重新保存，但**内容根本没变**，`Last-Modified` 时间也会更新，导致本可重用的缓存失效，造成不必要的重新下载。

### 4.2 ETag / If-None-Match (基于文件内容的唯一标识)

为了解决时间精度的缺陷，HTTP/1.1 引入了 ETag。

1.  **初次请求**：服务器在返回资源时，根据资源的内容生成一个**唯一标识符**（通常是一段哈希值/指纹），放在响应头的 `ETag` 字段中。只要文件内容改变，ETag 就会改变。
    - `ETag: "33a64df551425fcc55e4d42a148795d9f25f89d4"`
2.  **再次请求**：浏览器再次请求时，会在请求头中加上 `If-None-Match`，其值就是上次收到的 `ETag`。
3.  **服务器比对**：服务器拿 `If-None-Match` 的值与资源当前的 ETag 进行严格的字符串比对。
    - **如果匹配（说明内容没变）**：返回 **`304 Not Modified`**，浏览器继续用缓存。
    - **如果不匹配（说明内容变了）**：返回 `200 OK` 和新内容、新 ETag。

- **最高优先级**：在协商缓存中，**`ETag` 的优先级高于 `Last-Modified`**。如果同时存在，服务器会优先验证 ETag。

### 4.3 强势校验与弱势校验：`ETag` 前面为什么会多一个 `W/`

`ETag` 有两种形态，决定了比对的严格程度：

- `ETag: "abc123"` 是**强势校验 (Strong Validator)**：要求字节级完全一致。
- `ETag: W/"abc123"` 是**弱势校验 (Weak Validator)**，`W/` 即 weak：只要求**语义等价**，允许压缩、转码造成的字节差异。

这两个形态引出两个高频线上事故：

- **`ETag` 被中间环节改写，导致协商缓存永远不命中**：Nginx 默认按 `Last-Modified + Content-Length` 生成 ETag（本身就带 `W/`），CDN 开启压缩后往往还会再改写一次。一旦链路上有环节重写了 `ETag`，浏览器发出的 `If-None-Match` 和当前 `ETag` 就永远对不上，协商缓存退化成每次完整 `200`。
- **多机部署时 `ETag` 随机失效**：如果 `ETag` 由 `inode` 或高精度 `mtime` 生成，同一份文件在不同机器上算出的值不同；用户请求被负载均衡打到另一台机器，就会“命中不了”，表现为“**刷新一下快、刷新一下慢**”。正解是用**文件内容哈希**生成 `ETag`，或者干脆关掉 `ETag`、只保留 `Last-Modified`。

## 5. 缓存机制的完整流程梳理

1.  浏览器尝试发起一个 HTTP 请求。
2.  **检查强缓存**：浏览器查找本地是否存在该资源的缓存记录，并检查 `Cache-Control` (如 `max-age`) 判断是否过期。
    - **未过期**：命中强缓存，直接从本地读取并返回 `200 (from cache)`。请求结束。
    - **已过期（或存在 `no-cache`）**：强缓存失效，准备发起网络请求进行**协商缓存**。
3.  **准备协商请求**：浏览器提取之前保存的 `ETag` 和 `Last-Modified`，并将它们分别赋值给请求头 `If-None-Match` 和 `If-Modified-Since`，发送给服务器。
4.  **服务器验证（协商缓存）**：
    - 服务器优先比对 `ETag`，再比对 `Last-Modified`。
    - **未修改**：服务器认为客户端的缓存仍然可用，返回 **`304 Not Modified`**，只带头部，不带 Body。浏览器收到 304 后，更新本地缓存的过期时间，并从本地加载资源。请求结束。
    - **已修改（或服务器没有这两个验证头）**：缓存失效，服务器返回 **`200 OK`**，附带全新的资源数据以及新的缓存头信息。浏览器将新资源存入缓存。请求结束。

![浏览器缓存判定流程图](/img/browserCache.png)

## 6. 配置与验证

### 6.1 Nginx：三类资源三种策略

```nginx
# 1) 带 Hash 的构建产物：一年强缓存，连用户刷新都不回源
location ~* \.(?:js|css|woff2?|ttf|png|jpe?g|gif|svg|webp|avif)$ {
    add_header Cache-Control "public, max-age=31536000, immutable";
}

# 2) 入口 HTML：每次协商验证，绝不能走强缓存
location = /index.html {
    add_header Cache-Control "no-cache";
}

# 3) 接口：不进 CDN，且每次校验（敏感数据改成 no-store）
location /api/ {
    add_header Cache-Control "private, no-cache";
}
```

静态资源的托管与 `try_files` 路由兜底见 [部署环境与持续交付](/frontEngineering/ci-cd/deploymentEnvironments)；CDN 侧的缓存层级与刷新见 [CDN](/networkAndBrowsers/fundamentals/cdn)。

### 6.2 构建工具：靠文件名 Hash，而不是靠手动刷新缓存

```js
// vite.config.js —— 用内容哈希命名产物，把缓存问题变成“天然免疫”
import { defineConfig } from 'vite'

export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        entryFileNames: 'assets/[name].[hash].js',
        chunkFileNames: 'assets/[name].[hash].js',
        assetFileNames: 'assets/[name].[hash][extname]',
      },
    },
  },
})
```

- Webpack 的对应配置是 `output.filename: '[name].[contenthash].js'`。**要区分 `[hash]` 和 `[contenthash]`**：前者跟着整次构建走，后者只跟着文件内容走，用错了会让所有文件无谓地一起失效。
- **不要用 `?v=1` 这类查询参数当版本号**：部分代理与 CDN 的缓存策略会忽略查询串，改完照样拿旧文件；只有**路径本身变了**，才是一份真正的“**新资源**”。

### 6.3 验证：三个常用手段

1. **DevTools → Network → Size 列**：显示 `(memory cache)` / `(disk cache)` 是强缓存命中；`304` 是协商缓存命中；显示真实字节数则说明完整下载了一遍。
2. **勾选 Network 面板的 `Disable cache`**：只在该面板打开期间生效，它会绕过强缓存强制回源，适合对比“有 / 无缓存”的差异（它**不会**清掉 Service Worker 的 Cache Storage）。
3. **命令行验证响应头**：

```bash
# 看响应头是否真的带上了预期的 Cache-Control / ETag
curl -sI https://example.com/assets/app.8f3a91.js | grep -i 'cache-control\|etag'

# 模拟一次协商请求：带上上次拿到的 ETag，期望返回 304
curl -sI -H 'If-None-Match: "33a64df551425fcc55e4d42a148795d9f25f89d4"' https://example.com/index.html
```

## 7. 常见问题 (FAQ)

### 7.1 用户操作对缓存有什么影响？

- **地址栏输入 URL 回车 / 点击书签 / 点击超链接**：这是最正常的用户行为，浏览器会严格按照上述 **完整的缓存机制（强缓存优先，失效再协商）** 来执行。
- **按 F5 刷新页面 / 点击浏览器刷新按钮**：浏览器认为用户希望看到稍微新一点的内容。它会**强制忽略所有强缓存**（即便 `max-age` 没到期），向服务器发送请求。但在请求中依然会带上 `If-None-Match`/`If-Modified-Since`，也就是**只进行协商缓存**。如果资源没变，服务器依然返回 304。
- **按 Ctrl + F5 强制刷新 (Hard Reload)**：浏览器认为用户遇到了严重问题，必须获取绝对最新的版本。浏览器会**同时忽略强缓存和协商缓存**。它在发送请求时会带上 `Cache-Control: no-cache` 并且丢弃验证头（不带 If-None-Match），迫使服务器必须返回 `200 OK` 和完整的最新资源。

### 7.2 `from memory cache` 和 `from disk cache` 的区别是什么？

- **Memory Cache (内存缓存)**：读取速度极快，但持续性很短。通常用于缓存**正在当前网页上使用**的资源（如网页中加载好的多张图片、已经执行过的 JS/CSS 脚本）。**一旦你关闭了这个浏览器的 Tab 标签页，内存缓存就会被清空。**
- **Disk Cache (磁盘缓存)**：读取速度稍慢（因为需要读写硬盘），但容量大，且可以持久保存。哪怕你重启了电脑，只要缓存没过期，依然可以从磁盘中读取。大文件或者很少被修改的文件通常会被放入磁盘缓存。
- **分配策略**：浏览器有自己复杂的启发式算法来决定放哪里。一般来说，小的、频繁使用的资源放内存；大的、长期有效的放磁盘。

### 7.3 如何配置才能让前端发版（更新代码）后，用户立刻能看到最新效果，而不用手动清缓存？

这是前端工程化中最经典的问题。最佳实践是 **“长缓存时间 + 文件名哈希 (Hash)”** 组合拳：

- **对入口 HTML 文件**：通常配置**不进行强缓存**，每次都必须验证（`Cache-Control: no-cache`）甚至不缓存（`no-store`）。因为 HTML 文件的内容很少，每次下载不耗时，它是后续所有资源加载的起点。
- **对静态资源 (JS/CSS/图片等)**：在构建工具（如 Webpack/Vite）打包时，在文件名中加入基于内容的 Hash 值（例如 `app.v2a3bf1.js`）。
- **配置强缓存**：在服务器上为这些带 Hash 的静态资源配置**超长的强缓存时间**（例如一年：`Cache-Control: max-age=31536000, immutable`）。

### 7.4 为什么有了 ETag 还要保留 Last-Modified？

- 生成 ETag 通常需要读取文件内容并计算哈希值，这是一个消耗服务器 CPU 性能的操作，特别是对于体积很大的文件。
- 如果服务器判断出读取 Last-Modified 就能满足大部分场景的需求，可以节省计算 ETag 的开销。它们通常是搭配使用的，互为补充。

### 7.5 `Cache-Control: no-cache` 和 `Cache-Control: no-store` 的区别是什么？

- **`no-cache`**：代表客户端**可以**缓存资源，但在使用缓存前，**必须向服务器发送请求进行验证（即走协商缓存路线）**。如果服务器回复 304，就使用缓存；如果回复 200，就用新的。
- **`no-store`**：代表绝对禁止任何形式的缓存（包括浏览器、中间代理服务器、CDN 等）。每次请求都必须像第一次那样，从服务器拉取完整的资源。通常用于极度敏感的数据（如银行流水页面）。

### 7.6 `Cache-Control: max-age=0` 和 `no-cache` 有什么区别？

- **实际效果几乎一样**：两者都会让缓存立刻进入校验流程，都要发请求、都可能收到 304。
- **语义不同**：`max-age=0` 说的是“**这份缓存立即过期**”，逻辑上它仍是一份可缓存的响应，只是新鲜期为 0；`no-cache` 说的是“**禁止直接使用缓存，每次使用前必须验证**”。
- **实践建议**：需要每次校验就写 `no-cache`。`max-age=0` 更常见于某些老框架或中间件的默认值，容易被误读成“**不缓存**”；真正表达“**禁用**”的是 `no-store`。

### 7.7 我改了 URL 参数（或者发了新版本），为什么还是旧内容？

- **浏览器强缓存**：你改的只是查询串、路径没变，本地直接命中强缓存，压根没发请求（Network 面板里会显示 `(from disk cache)`）。
- **Service Worker**：它比 HTTP 缓存更靠前，旧 SW 会直接把缓存返回给你，见 [PWA 与 Service Worker 缓存](/networkAndBrowsers/caching/serviceWorkerPwa)。
- **CDN / 反向代理**：源站已经更新，但边缘节点还没到期，响应头 `Age` 能看出它已经缓存了多久，需要做刷新或预热。
- **上游代理**（公司网关、运营商透明代理）：最不可控，只能靠**带 Hash 的 URL 路径**绕开。

### 7.8 既然命中 304，为什么还要花时间？它到底省了什么？

304 省掉的只是**响应体的传输**：这一轮请求-响应往返、以及服务端的一次读取或计算都还在。所以协商缓存的收益与文件体积成正比——**大文件很划算，小文件有时甚至不划算**（304 没有正文，浏览器无法提前开始接收 body，某些场景下反而不如直接 200 来得快）。这也是「纯静态且体积小的资源更适合 `immutable`」的原因。
