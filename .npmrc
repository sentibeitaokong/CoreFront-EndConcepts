# 强制开启 node-linker，确保在 pnpm 11 下，依赖图谱与 Vite 预构建逻辑完全吻合
node-linker=hoisted
# 启用内容寻址带来的硬链接，大幅提升 build 速度
shamefully-hoist=true
# 自动提升 peerDependencies，防止 FontAwesome 等包在 Node 22 下找不到依赖
auto-install-peers=true